---
title: 'Input File'
description: 'Input File with curd file (image).'
published: true
links:
  - title: 'Home'
    url: '/'
  - title: 'About'
    url: '/about'
slug: 'input-file'
---

## Installation

<ComponentPreview name="input-file" />

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://std-ui.vercel.app/r/input-file
```

</TabsContent>

</Tabs>

## Usage

```tsx
import { InputFile } from '@/components/ui/input-file'
```

<br />
```tsx
  const [files, setFiles] = React.useState<File[] | null>(null)

  const handleFileChange = (selectedFiles: File[] | null) => {
    setFiles(selectedFiles)
  }

  <InputFile id="file-upload" name="file" accept="image/*" onChange={handleFileChange} />
```

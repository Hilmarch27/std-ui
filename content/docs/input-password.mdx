---
title: "Input Password"
description: "Input password with show or hide field"
published: true
links:
  - title: "Home"
    url: "/"
  - title: "About"
    url: "/about"
slug: "input-password"
---

## Installation

<ComponentPreview name="input-password" />

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://std-ui.vercel.app/registry/input-password
```

</TabsContent>

</Tabs>

## Usage

```tsx
import InputPassword from "@/components/ui/input-password";
```

<br />
```tsx
const Preview = () => {
  const [password, setPassword] = React.useState("");
  return (
    <div className="flex flex-row space-x-3 items-center">
      <InputPassword
        placeholder="Password"
        className="mt-2"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
      />
      <Button variant={"outline"} className="mt-2" onClick={() => alert(password)}>
        Submit
      </Button>
    </div>
  );
};
```
```
